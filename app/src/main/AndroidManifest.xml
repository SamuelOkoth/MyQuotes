<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moringa.myquotes">

    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="MainActivity">
        </activity>
        <activity android:name=".SavedReadListActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".models.ReadActivity" />
        </activity>
        <activity android:name="CreateAccountActivity" />
        <activity android:name="LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".models.ReadActivity"/>
        <activity android:name=".AddActivity"/>
        <activity android:name=".PicActivity"/>
        <activity android:name=".ReadDetailActivity"/>
    </application>


</manifest>